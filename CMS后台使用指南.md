# CMS 后台使用指南

> 适用于：运营/编辑人员（无需编程知识）

---

## 一、怎么打开后台

1. 打开浏览器，访问：**`https://你的域名.com/admin/`**
2. 首次访问会跳转到 GitHub 登录页面
3. 用你的 GitHub 账号登录（需要管理员提前给你开通权限）
4. 登录成功后自动回到后台管理界面

---

## 二、后台能做什么

| 功能 | 说明 |
|---|---|
| **产品管理** | 新增、修改产品信息（名称、价格、图片、规格等） |
| **博客管理** | 发布、编辑行业文章和公司动态 |
| **图片上传** | 直接上传图片到云存储，自动生成链接 |
| **SEO 设置** | 修改首页和产品页的搜索引擎标题和描述 |

---

## 三、产品管理（最常用）

### 3.1 查看产品列表

左侧菜单点击 **「Product Management」**，可以看到所有产品。

### 3.2 修改已有产品

1. 在产品列表里找到要修改的产品，点击标题
2. 修改对应的字段（价格、库存、图片等）
3. 改完后，点击右上角 **「Save」** 保存
4. 保存后会生成一个草稿，状态变为 **「In Review」**
5. 等待管理员审核通过后，修改会自动上线

### 3.3 新增产品

1. 点击右上角 **「New Product」** 按钮
2. 填写以下必填信息：

| 字段名 | 说明 | 示例 |
|---|---|---|
| **Product Name** | 产品名称 | `Professional UV/LED Nail Lamp 168W` |
| **SKU Code** | 产品编号 | `n0001` |
| **Category** | 所属分类 | 下拉选择，如 `Nail Supplies` |
| **Wholesale Price (USD)** | 批发价格（美元） | `12.5` |
| **Main Product Image** | 产品主图 | 点击上传或选择已有图片 |
| **Product Description** | 产品描述 | 写详细的卖点和参数介绍 |

3. 以下是选填信息：

| 字段名 | 说明 |
|---|---|
| **Marketing Subtitle** | 营销副标题（首页特色卡片会显示） |
| **Subcategory** | 子分类，如 equipment, lashes |
| **Stock Quantity** | 库存数量 |
| **MOQ (text)** | 起订量，如 `50 pcs` |
| **Additional Images** | 更多图片（可上传多张） |
| **Badge** | 角标，如 Hot Seller, Bestseller, New Arrival |
| **Discount (%)** | 折扣百分比 |
| **Product Specifications** | 规格参数（可添加多行，每行一个标签+值） |

4. 填写完成后点击 **「Save」** → 进入待审核状态

### 3.4 产品字段详细说明

#### 图片上传

- 点击图片框 → 选择本地图片 → 自动上传到云存储
- 上传后输入框会自动填入图片链接，**不要手动修改**
- 支持多图：点击 **「Additional Images」** 下的 **「Add」** 逐个添加

#### 产品描述（Markdown 格式）

描述框支持简单的排版：

```markdown
这是一段介绍文字。

### 产品特点
- 高功率 168W
- 33 颗 LED 灯珠
- 智能感应开关

### 适用场景
适合专业美甲沙龙和家庭使用。
```

- `###` 开头 = 小标题
- `-` 开头 = 列表项
- 空行 = 分段

#### 规格参数

点击 **「Product Specifications」** 下的 **「Add」**，每行填一个参数：
- **Label**：参数名（如 `Power`）
- **Value**：参数值（如 `168W`）

---

## 四、博客管理

### 4.1 写新文章

1. 左侧菜单点击 **「Blog Management」**
2. 点击 **「New Blog Post」**
3. 填写：

| 字段 | 说明 |
|---|---|
| **Title** | 文章标题 |
| **Publish Date** | 发布日期 |
| **Author** | 作者（默认 AVGM Team） |
| **Cover Image** | 封面图 |
| **Description** | 文章摘要（显示在列表卡片上） |
| **Content Body** | 正文（Markdown 格式，同上） |

4. 点击 **「Save」** 保存

### 4.2 编辑已有文章

同产品编辑流程：找到文章 → 修改 → 保存 → 等待审核。

---

## 五、图片管理

后台内置了云图床，所有上传的图片会自动存储在 Cloudflare R2。

### 上传方式

- 在任何图片字段中，点击组件 → 选择本地文件
- 支持拖拽上传
- 上传完成后，字段自动填入云存储链接（以 `https://` 开头）

### 注意事项

- **不要删除已使用的图片**，否则网站上对应位置会显示裂图
- 推荐图片尺寸：
  - 产品主图：**800×800** 以上，正方形
  - 博客封面：**1200×630**
  - 产品描述里的图片：**800 宽度**

---

## 六、审核工作流

为了保护网站安全，所有修改需要经过审核才能上线：

```
编辑人员保存 → 状态变为 "In Review" → 管理员审核 → 点击 "Publish" → 修改上线
```

### 编辑人员（你）

1. 新增或修改内容后点击 **「Save」**
2. 系统自动生成草稿，状态显示为 **「In Review」**
3. 此时内容**还没有上线**，只有你自己能看到
4. 通知管理员去审核

### 管理员

1. 进入后台，顶部点击 **「Workflow」** 选项卡
2. 查看待审核的内容
3. 确认无误后点击 **「Publish」**
4. 内容自动合并到主分支，**1-2 分钟后网站自动更新**

---

## 七、常见问题

### Q: 我不小心保存了，怎么撤销？

在列表中点击草稿条目，修改回原来的内容再保存即可。

### Q: 为什么我不能删除产品？

为了安全，后台**不提供删除按钮**。如果需要下架某个产品，可以联系管理员在 GitHub 上处理。

### Q: 改了内容为什么网站上没变化？

可能原因：
1. 内容还在 **「In Review」** 状态，管理员还没审核发布
2. 刚发布，需要等 1-2 分钟 Vercel 自动构建部署
3. 浏览器缓存，按 `Ctrl + F5` 强制刷新

### Q: 上传图片失败了？

检查：
1. 图片文件大小是否超过 **5MB**
2. 图片格式是否为 JPG / PNG / WebP
3. 网络是否正常

### Q: 打不开后台 / 登录失败？

确认：
1. 网址是否正确（`/admin/` 结尾）
2. GitHub 账号是否已被管理员添加为仓库 Collaborator
3. 浏览器是否禁用了第三方 Cookie（需要允许）

---

## 八、快速参考卡

| 我要做什么 | 操作路径 |
|---|---|
| 加新产品 | Product Management → New Product → 填表 → Save |
| 改价格 | Product Management → 点产品 → 改 Price → Save |
| 换图片 | Product Management → 点产品 → 点图片框 → 选新图 → Save |
| 发文章 | Blog Management → New Blog Post → 填内容 → Save |
| 改 SEO 标题 | SEO Settings → 点对应页面 → 修改 → Save |

---

> 有不懂的地方？问管理员或者直接在企业微信群里问。
